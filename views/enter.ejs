<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SimpleBoard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: rgb(227, 227, 227);
        }

        form {
            width: 75%;
            height: 80%;
            margin: 30px auto;
            padding: 20px;
            background-color: white;
            border: 2px solid rgb(197, 197, 197);
            /* border-radius: 10px; */
        }

        label {
            font-size: 25px;
        }

        #inputTitle {
            font-size: 30px;
            border-radius: 10px;
        }

        textarea[name="content"] {
            font-size: 25px;
            border-radius: 10px;
            white-space:pre;
        }

        input[type='date'] {
            font-size: 25px;
            border-radius: 10px;
        }

        button[type='submit'] {
            font-size: 25px;
            border-radius: 10px;
        }

        #author {
            font-size: 25px;
        }

        #welcomeBox {
            border: 2px solid rgb(184, 184, 184);
            width: 30%;
            margin: auto;
            margin-top: 10%;
            padding: 20px;
            padding-bottom: 10px;
            /* border-radius: 10px; */
            background-color: rgb(244, 244, 244);
        }

        button {
            font-size: 20px;
        }

        h5 {
            margin-top: 10px;
        }

        #loginBtn {
            font-size: 20px;
        }

    </style>
</head>
<body>
    <%- include('menu.html') %>

    <% if (user) { %>
        <form action="/save" method="post">
            <div class="mb-3">
                <label class="form-label" for="inputTitle">제목</label>
                <input required type="text" class="input form-control" id="inputTitle" name="title">
            </div>
            <p></p>
            <div class="mb-3">
                <label class="form-label">내용</label>
                <textarea required name="content" cols="16" wrap="hard" class="form-control" rows="8"></textarea>
            </div>
            <p></p>
            <div class="mb-3">
                <label class="form-label">작성자</label>
                <input readonly id="author" type="text" class="form-control" name="author" value="<%= user.userid %>">
            </div>

            <div class="mb-3">
                <label class="form-label">작성일</label>
                <input readonly type="date" class="form-control" name="someDate" id="inputDate">
            </div>
            <button type="submit" class="btn btn-warning">저장</button>
        </form>
    <% } else { %>
        <div id="welcomeBox">
            <h3>SimpleBoard</h3>
            <h5>로그인 후 이용해주세요.</h5>
            <button id="loginBtn" class="btn btn-warning" onclick="location.href='/login'">login</button>
        </div>
    <% } %>
    <script>
        document.getElementById('inputDate').value = new Date().toLocaleDateString('en-CA');
    </script>
</body>
</html>