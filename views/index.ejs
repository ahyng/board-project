<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        #welcomeBox {
            border: 2px solid rgb(184, 184, 184);
            width: 30%;
            margin: auto;
            margin-top: 10%;
            padding: 20px;
            padding-bottom: 10px;
            /* border-radius: 10px; */
        }

        button {
            font-size: 20px;
        }

        h5 {
            margin-top: 10px;
        }

        #btns {
            display: flex;
        }

        #logout {
            margin-right: 10px;
        }


    </style>
</head>
<body>
    <%- include('menu.html') %>
    
    <div id="welcomeBox">
        <% if (user) { %>
            <h3>반갑습니다. <%= user.userid %>님</h3>
            <div id="btns">
                <button id="logout" class="btn btn-warning" onclick="location.href='/logout'">logout</button>
                <button class="btn btn-secondary" onclick="deleteUser()">계정 삭제</button>
            </div>
        <% } else { %>
            <h3>SimpleBoard</h3>
            <h5>로그인 후 이용해주세요.</h5>
            <button class="btn btn-warning" onclick="location.href='/login'">login</button>
        <% } %>
    </div>
    <script>
        function deleteUser() {
            let check = confirm('정말 삭제하시겠습니까?');
            if (check) {
                window.location.href = '/delete_user';
            }
        }
    </script>
</body>
</html>