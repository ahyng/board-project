<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SimpleBoard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>

        #contentBox{
            width: 75%;
            margin: 30px auto;
            border: 2px solid rgb(179, 179, 179);
        }

        #contentBox h2 {
            font-size: 35px;
            padding: 15px 15px 15px 5px;
        }

        #contentBox div {
            font-size: 25px;
        }

        #content {
            padding: 15px;
            height: 58vh;
            white-space:pre;
            max-height: 60vh;
            overflow-y: auto;
        }

        #cardFooter {
            padding: 15px;
        }

        #savedDate {
            padding-bottom: 10px;
        }

        #editBtn {
            font-size: 20px;
        }

        #deleteBtn{
            font-size: 20px;
        }

        #backBtn {
            font-size: 20px;
        }

        #commentTitle {
            width: 75%;
            margin: auto;
        }

        #commentTitle h3 {
            margin-left: 10px;
            margin-bottom: 30px;
        }

    </style>
</head>
<body>
    <%- include('menu.html') %>

    <% if (user) { %>

        <div class="card " id="contentBox">
            <div class="card-header">
                <h2><%= data.title %></h2>
                <h5>작성자 : <%= data.author %></h5>
            </div>
            <div class="card-body" id="content"><%= data.content %></div>
            <div class="card-footer" id="cardFooter">
                <div id="savedDate">작성일: <%= data.date %></div>
                <% if (user.userid == data.author) { %>
                    <button id="editBtn" type="button" class="btn" style="background-color: #20c997; color: white;" onclick="location.href='/edit/<%= data._id %>'" >수정하기</button>
                    <button id="deleteBtn" type="button" class="delete btn" style="background-color: #ff3549; color: white;" data-id="<%= data._id %>">삭제</button> 
                <% } %>
                <button id="backBtn" type="button" onclick="window.history.back()" class="btn" style="background-color: #96a1ad; color: white;">목록으로 이동</button>
            </div>
        </div>

        <div id="commentTitle">
            <h3>댓글(<%= data.comment? data.comment.length : 0 %>)</h3>
        </div>

        <% if (data.comment) { %>
            <% for (let i=0; i < data.comment.length; i++){ %>
            <div id="coments">
                <div class="card w-75" style="margin: auto; font-size: 25px; margin-bottom: 20px;">
                    <div class="card-body">
                    <h5>작성자 : <%= data.comment[i].id %></h5>
                    <%= data.comment[i].message %>
                    </div>
                </div>
            </div>
            <% } %>
        <% } %>

        <form action="/comment" method="post" class="mb-3" style="width: 75%; margin: auto;">
            <input style="display: none;" name="postId" value="<%= data._id %>" />
            <input style="display: none;" name="userid" value="<%= user.userid %>" />
            <label for="exampleFormControlTextarea1" class="form-label" style="font-size: 20px;"><%= user.userid %></label>
            <textarea name="commentMessage" class="form-control" id="exampleFormControlTextarea1" rows="3" style="font-size: 20px;"></textarea>
            <button type="submit" class="btn btn-primary">저장</button>
        </form>



    <% } else { %>
        <%- include('welcome-box.ejs') %>
    <% } %>
    <script src="https://code.jquery.com/jquery-1.12.4.min.js"></script>
    <script>
        $('.delete').click((e) => {
            let check = confirm('정말 삭제하시겠습니까?');
            if (check) {
                let sid = e.target.dataset.id;
                let item = $(e.target);
                $.ajax({
                    type: 'post',
                    url: '/delete',
                    data: {_id : sid}
                }).done((result) => {
                    item.closest('tr').remove();
                    alert('삭제가 완료되었습니다.');
                    location.href = '/list';
                }).fail((xhr, textStatus, errorThrown) => {
                    console.log('게시물 삭제 실패');
                    console.log(xhr, textStatus, errorThrown);
                });
            }  
        });
    </script>
</body>
</html>